# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Scraper working directory (raw data before copy)
/data/

# Translation cache — persisted via GitHub Actions cache, not git
# (seeded from git history on cache miss; see deploy.yml)
/translation_cache.json

# IDE
.cursor/

# Claude Design handoff bundles (extracted locally, not source)
.design-handoff*/

# OS
Thumbs.db
.DS_Store
